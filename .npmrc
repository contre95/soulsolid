# Suppress npm's "> pkg@ver script" lifecycle banners.
# devenv's enterShell hook captures and re-evals build output, and those
# "> ..." lines get parsed as shell redirections, creating stray empty
# files (mkdir, npm, npx, soulsolid@1.0.0). Silencing the banner stops it.
loglevel=silent
